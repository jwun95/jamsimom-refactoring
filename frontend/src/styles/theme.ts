import { Theme } from "@emotion/react";

const theme: Theme = {
  colors: {
    mainColor: "#FCB25B",
    mainColorLight: "#FFD29D",
    subColor: "#5A2F0F",
    black: "#000",
    white: "#ffffff",
  },
};

export default theme;
